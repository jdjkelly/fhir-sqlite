{
  "name": "fhir-sqlite",
  "type": "module",
  "private": true,
  "scripts": {
    "db:schema": "bun generate.ts > schema.sql",
    "db:create": "bun load.ts",
    "db:load": "bun load.ts",
    "db:import": "bun import.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "@atomic-ehr/fhir-canonical-manager": "^0.0.15"
  },
  "fcm": {
    "packages": [
      "hl7.fhir.r4.core"
    ],
    "registry": "https://fs.get-ig.org/pkgs/"
  }
}
